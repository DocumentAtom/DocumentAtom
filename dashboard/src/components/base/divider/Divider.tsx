import { Divider, DividerProps } from "antd";
import React from "react";

const DocuAtomDivider = (props: DividerProps) => {
  return <Divider {...props} />;
};

export default DocuAtomDivider;
